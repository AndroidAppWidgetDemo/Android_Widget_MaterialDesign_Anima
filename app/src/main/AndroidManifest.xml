<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunzxyong.materialdesignanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImageActivity"></activity>
        <activity android:name=".ImageDetailActivity"></activity>
        <activity android:name=".RecyclerViewActivity"></activity>
        <activity android:name=".SlideActivity"></activity>
        <activity android:name=".FadeActivity"></activity>
        <activity android:name=".ExplodeActivity"></activity>
    </application>

</manifest>
